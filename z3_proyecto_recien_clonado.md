## 1. proyecto clonado
- se borra el archivo package-lock.json y la carpeta node_modules
- npm install  para crear la carpeta de las librerias
- npm install --legacy-peer-deps si da errores de librerias para que de todas maneras haga la instalacion

"scripts": {
    "dev": "vite",  // para correrlo (npm run dev) 
    "start": "vite --open",  // para correrlo con npm start
  },